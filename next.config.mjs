/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "morar.com.br",
      "atsloanestable.com",
      "www.simplyrecipes.com",
      "assets.bonappetit.com",
    ],
  },
};

export default nextConfig;
